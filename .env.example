# ============================================================================
# Environment Variables Configuration
# ============================================================================
# All environment variables are loaded dynamically at runtime.
# No hardcoded values exist in the codebase.
#
# Set these variables in:
# - Supabase: Dashboard → Settings → API (source of truth)
# - Vercel: Dashboard → Settings → Environment Variables
# - GitHub Actions: Repository → Settings → Secrets and variables → Actions
# - Local: .env.local file (not committed to git)
# ============================================================================

# ============================================================================
# SERVER-SIDE ENVIRONMENT VARIABLES (Never expose to client)
# ============================================================================

# ===== Core Supabase (Server) =====
# Required: Get from Supabase Dashboard → Settings → API
# These are the source of truth for all Supabase configuration
SUPABASE_URL=https://{project-ref}.supabase.co
SUPABASE_ANON_KEY=your-anon-key-here
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here
SUPABASE_JWT_SECRET=your-jwt-secret-here
SUPABASE_PROJECT_REF={project-ref}
SUPABASE_ACCESS_TOKEN=your-supabase-access-token-here

# ===== Database =====
# Required: Construct using your Supabase service role key
# Format: postgresql://postgres:{service-role-key}@db.{project-ref}.supabase.co:5432/postgres?sslmode=require
DATABASE_URL=postgresql://postgres:${SUPABASE_SERVICE_ROLE_KEY}@db.{project-ref}.supabase.co:5432/postgres?sslmode=require
DIRECT_URL=postgresql://postgres:${SUPABASE_SERVICE_ROLE_KEY}@db.{project-ref}.supabase.co:5432/postgres?sslmode=require

# ===== Prisma =====
PRISMA_CLIENT_ENGINE_TYPE=wasm

# ===== Application Configuration (Server) =====
# Required for production: Set to your production URL
NEXTAUTH_URL=https://your-app.vercel.app
# Generate with: openssl rand -base64 32
NEXTAUTH_SECRET=generate-with-openssl-rand-base64-32
LOG_LEVEL=info
NODE_ENV=production

# ===== OAuth Providers =====
# Optional: Enable OAuth providers as needed
GITHUB_CLIENT_ID=
GITHUB_CLIENT_SECRET=
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=

# ===== Stripe Payment Processing =====
# Required for payment/subscription features
STRIPE_SECRET_KEY=sk_test_...
STRIPE_WEBHOOK_SECRET=whsec_...

# ===== Storage Configuration =====
# Optional: Storage bucket configuration
SIGNING_SECRET=

# ===== AI/ML Services =====
# Optional: Required for AI features (embeddings, etc.)
OPENAI_API_KEY=sk-...

# ===== CI/CD Configuration =====
# Required for GitHub Actions and Vercel CLI
VERCEL_TOKEN=your-vercel-token

# ===== Redis/Caching =====
# Optional: For distributed caching and rate limiting
REDIS_URL=redis://localhost:6379

# ===== Admin Access =====
# Optional: Basic Auth for admin dashboard (format: username:password)
ADMIN_BASIC_AUTH=admin:secure-password

# ===== Preview Protection =====
# Optional: Require auth for preview deployments
PREVIEW_REQUIRE_AUTH=true

# ===== Canary Deployment =====
# Optional: Feature flags for canary deployments
CANARY_CHECKOUT_ENABLED=false
CANARY_CHECKOUT_PERCENTAGE=0

# ===== Monitoring & Observability =====
# Optional: For external monitoring services
SENTRY_DSN=
DATADOG_API_KEY=
NEW_RELIC_LICENSE_KEY=
OTEL_SERVICE_NAME=aias-platform
OTEL_EXPORTER_OTLP_ENDPOINT=
ENABLE_OTEL=true

# ===== Email Configuration =====
# Optional: For transactional emails
RESEND_API_KEY=
SMTP_HOST=
SMTP_PORT=
SMTP_USER=
SMTP_PASSWORD=

# ===== GitHub Integration =====
# Optional: For GitHub Actions and integrations
GITHUB_TOKEN=
GITHUB_OWNER=your-org
GITHUB_REPO=aias-platform

# ===== Vercel Integration =====
# Optional: For Vercel integrations
VERCEL_PROJECT_ID=

# ===== Health Check Configuration =====
# Optional: For smoke tests and health checks
HEALTH_URL=http://localhost:3000/api/healthz

# ===== Additional Service-Specific =====
# Optional: Additional service configurations found in codebase
META_ACCESS_TOKEN=
META_AD_ACCOUNT_ID=
TIKTOK_ACCESS_TOKEN=
TIKTOK_ADVERTISER_ID=
SHOPIFY_API_KEY=
SHOPIFY_PASSWORD=
SHOPIFY_STORE=
SLACK_WEBHOOK_URL=
TELEMETRY_WEBHOOK_URL=
ERROR_WEBHOOK_URL=
RELIABILITY_ALERT_WEBHOOK=
ORCHESTRATOR_BUDGET=75
ORCHESTRATOR_RELIABILITY_THRESHOLD=99.9
ORCHESTRATOR_AUTO_PR=false
ORCHESTRATOR_AUTO_FIX=false

# ============================================================================
# PUBLIC ENVIRONMENT VARIABLES (Exposed to client via NEXT_PUBLIC_*)
# ============================================================================

# ===== Core Supabase (Public) =====
# Required: These are exposed to the browser/client
# Must match server-side SUPABASE_URL and SUPABASE_ANON_KEY
NEXT_PUBLIC_SUPABASE_URL=https://{project-ref}.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key-here

# ===== Application Configuration (Public) =====
NEXT_PUBLIC_APP_ENV=production
NEXT_PUBLIC_SITE_URL=https://your-app.vercel.app
NEXT_PUBLIC_APP_URL=https://your-app.vercel.app

# ===== Stripe Payment Processing (Public) =====
# Required for payment/subscription features
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=pk_test_...

# ===== Storage Configuration (Public) =====
# Optional: Storage bucket configuration
NEXT_PUBLIC_UPLOAD_BUCKET=public

# ===== Stripe Price IDs (Public) =====
# Optional: Stripe price IDs for different plans
NEXT_PUBLIC_STRIPE_PRICE_STARTER_MONTHLY=
NEXT_PUBLIC_STRIPE_PRICE_STARTER_YEARLY=
NEXT_PUBLIC_STRIPE_PRICE_PRO_MONTHLY=
NEXT_PUBLIC_STRIPE_PRICE_PRO_YEARLY=
NEXT_PUBLIC_STRIPE_PRICE_ENTERPRISE_MONTHLY=
NEXT_PUBLIC_STRIPE_PRICE_ENTERPRISE_YEARLY=
NEXT_PUBLIC_STRIPE_PRICE_LEAD_SCORING=
NEXT_PUBLIC_STRIPE_PRICE_EMAIL_SEQUENCES=
NEXT_PUBLIC_STRIPE_PRICE_MEETING_SCHEDULER=
NEXT_PUBLIC_STRIPE_PRICE_DATA_MIGRATION=
NEXT_PUBLIC_STRIPE_PRICE_REPORT_GENERATOR=

# ===== Additional Public Configuration =====
# Optional: Additional public configurations
NEXT_PUBLIC_WS_URL=
NEXT_PUBLIC_VERCEL_ENV=
NEXT_PUBLIC_APP_ID=generic
EXPO_PUBLIC_TELEMETRY=false

# ============================================================================
# Security Notes:
# - Never commit .env files to git
# - Rotate secrets regularly
# - Use different values for staging/production
# - Store sensitive values in Vercel/GitHub Secrets
# - Never expose server-side secrets (SUPABASE_SERVICE_ROLE_KEY, etc.) to client
# ============================================================================
